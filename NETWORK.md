# 展会网络方案与公司内模拟（现场网络）

本文管演示现场的**网络形态**：展会目标拓扑、在公司如何 1:1 模拟、以及为什么网段不能随便改。部署与编排纪律见 [`MANAGEMENT.md`](MANAGEMENT.md)，服务探活见 [`tools/health/README.md`](tools/health/README.md)。

## 1. 结论先行

**演示网必须完整复刻 `192.168.0.0/24`，且演示路由器 LAN 口地址必须是 `192.168.0.1`。** 因为 5090 与秤都是**静态地址**（见 §2），网段一改两台都失联。

由此推出一条硬约束：**演示路由器的 WAN 口绝不能接公司墙口**——公司网也是 `192.168.0.0/24`，WAN 与 LAN 同网段会让路由器报错或行为诡异。外网只能走独立通道（手机热点 / 随身 4G / USB 4G 网卡，其网段天然不是 `192.168.0.x`）。

## 2. 目标拓扑（展会 = 公司模拟，形态完全一致）

```
       秤(四通道模块) ──→ 交换机 ──┐
                                  │
       5090 服务器 ───────────────┼──→ 演示路由器 ──→ 外网
                                  │   LAN 192.168.0.1  (展会: 现场网线)
       开发机 / 演示手机 / 项链 ───┘   (WiFi + LAN 口)   (公司: 热点/4G)
```

公司模拟与展会的**唯一差别是 WAN 接什么**，LAN 侧一根线、一个地址都不用变。这就是这套编址的全部价值。

## 3. 为什么网段不能改：两台关键设备是静态地址

| 设备 | MAC | 地址配置 | 配置位置 |
|---|---|---|---|
| 5090 服务器 | `a0:ad:9f:a4:1d:b4` | 静态 `192.168.0.50/24`，网关 `192.168.0.1`，DNS `192.168.0.1` + `223.5.5.5` + `119.29.29.29` | NetworkManager 连接 `Wired connection 1`（`ipv4.method=manual`），接口 `eno1` |
| 四通道秤模块 | `d4:ad:20:ef:cf:8d` | 静态 `192.168.0.80/24`，网关 `192.168.0.1`，DNS `208.67.222.222` | 设备内置 web（`http://192.168.0.80/`，Basic 认证 `admin/admin`，`network.shtml`，`staticip=1`） |

两个推论，都是实操上会咬人的地方：

- **不需要在演示路由器上做 DHCP 静态绑定。** 两台设备自带静态地址，插上就是 `.50` 和 `.80`。
- **LAN 口地址必须是 `192.168.0.1`。** 两台设备的网关都写死指向它。若 LAN 口设成别的（如 `.254`），局域网内仍能互通（同网段直连不经网关），但**两台设备都上不了外网**，5090 的 DNS 也会失效——表现为"内网都好、就是拉不到外部服务"，很容易误判成服务故障。

开发机与演示手机没有这个约束，走 DHCP 即可。

> **当前实际状态（2026-08-05）**：秤已被临时改到 `192.168.100.80/24`、网关 `192.168.100.1`，位于测试路由器的网络内；5090 经公司网关有路由可达（`192.168.100.80 via 192.168.0.1`）。这是过渡状态，按本文方案最终应改回 `192.168.0.80`。改地址要趁它的 web 还够得着——**先改 IP、再拆路由器**，否则它带着 `192.168.100.80` 落到 `192.168.0.0/24` 里就成了孤岛，只能拿台电脑改成同网段直连才能救。

## 3.1 跨网段访问的首包延迟（会伪装成"秤离线"）

**跨网段访问时，新建 TCP 连接后的第一个 Modbus 请求约需 6.4 秒才应答；同一条连接上的后续请求稳定在 20 毫秒。** 同网段时（8/4 在 `192.168.0.80`）没有这个现象，所以它是跨网段/经网关才出现的。

这个特性极具迷惑性，2026-08-05 排查时的表现是：

- TCP 握手秒级完成、HTTP 配置页一切正常，唯独 Modbus 一声不吭；
- 秤会重发 SYN-ACK、对请求迟迟不 ACK，看起来像"设备卡死"；
- 实际只是**客户端在设备应答前就超时断开了**——抓包里能看到秤 ACK 数据和我方发 FIN 相差仅 52 微秒。

因此 `dx_backend.py` 的秤链路已改为**持久连接**：`_ScaleLink` 保持一条常连，首个请求用 `SCALE_FIRST_TIMEOUT=15s`、热连接后用 `SCALE_READ_TIMEOUT=3s`，失败则关闭连接下轮重连并逐步退避。**每次读都新建连接 + 短超时的写法在这里必然失败**，旧实现（每次 `create_connection` + 1.2s 超时）就是这么"读不到"的。

排查时也要注意：**不要反复猛建连接**。模块的连接槽位有限，连爆之后新连接虽然能完成 TCP 握手却不会被应用层 accept，会让人误判成设备故障。诊断时用一条连接、给足超时。

## 4. 一旦改网段，需要同步改的位置

留作决策依据：**换网段不是改一行的事**，而且其中一项必须重新装机。

| 位置 | 影响 |
|---|---|
| `local-stack/superadmin/superadmin.conf` 的 `proxy_pass http://192.168.0.50:8070` | superadmin 深体验区反代 dx-backend，不改即 504 |
| `tools/qwen_tunnel_keeper.py`（`TUNNEL_APP` 与 ssh 目标） | 开发机 launchd 常驻的 Qwen 隧道守护会失效 |
| `local-stack/lumen/scripts/deploy-5090.sh` | 部署目标，可用环境变量 `LUMEN_DEPLOY_TARGET` 覆盖 |
| `dx_backend.py` 的 `SCALE_HOST` | 秤地址 |
| **手机 App 的 `IMAGE_RELAY_URL`** | **最贵**：由 `react-native-config` 从 `.env` 读、**编译进 App**，改地址必须重跑 ios-build 并重新装机 |
| `ssh-5090` / `ifa-health-check` 两个 Claude skill、各 README | 不影响运行，但会持续误导排障 |

## 5. 搭建步骤

先决条件：准备好独立外网通道（手机热点 / 随身 4G）。**全程不要把演示路由器的 WAN 口插到公司墙口。**

1. **配演示路由器**（先不接任何上行，用网线直连它的 LAN 口配置）
   - LAN 网段 `192.168.0.0/24`，LAN 口地址 `192.168.0.1`，掩码 `255.255.255.0`
   - DHCP 开启，地址池建议 `192.168.0.100–192.168.0.200`（避开静态占用的 `.50` / `.80`）
   - 配好 WiFi（演示手机、项链、开发机都连它）
2. **接外网**：WAN 口接热点 / 4G 网卡。确认路由器已拿到 WAN 地址且不是 `192.168.0.x`。
3. **接设备**：秤 → 交换机 → 演示路由器 LAN 口；5090 用网线接 LAN 口或同一交换机；开发机与手机连它的 WiFi。
4. **验证**：跑 §6 全部检查项。

## 6. 验证清单

```bash
# 1. 本机在演示网内（应为 192.168.0.x，网关 192.168.0.1）
ipconfig getifaddr en0
netstat -rn -f inet | awk '$1=="default"{print "网关:", $2}'

# 2. 秤在同一二层网络内 —— 这是"是否被路由器隔断"的决定性判据
#    （秤当前临时在 192.168.100.80，见 §3.1；下面按目标状态 192.168.0.80 写）
arp -an | grep -i 'd4:ad:20' && echo "秤在本网段" || echo "秤不在本网段：检查它是否被接到了别的路由器后面"

# 3. 秤硬件与网络（绕过服务直连 Modbus）
#    注意：502 端口可达 ≠ 能读到数据，跨网段首包要等约 6 秒，见 §3.1
nc -z -G 3 192.168.0.80 502 && echo "Modbus 502 可达"

# 4. 四通道读数（经 dx-backend，四个通道都应有 net/gross）
curl -s http://192.168.0.50:8070/api/food-scales | python3 -m json.tool

# 5. 5090 演示服务端口
for p in 22 18090 18091 8060 8070 3001 8000; do
  nc -z -G 3 192.168.0.50 $p && echo "$p 通" || echo "$p 不通"
done

# 6. 外网通路（5090 侧，DNS 与出网一起验）
ssh odyss@192.168.0.50 'curl -s -o /dev/null -w "%{http_code}\n" -m 8 https://www.google.com/generate_204'
```

第 2 项若显示"不在本网段"，不要去查服务和代码——先查线。

## 7. 事故复盘（2026-08-05，本文档的由来）

**现象**：四个秤全部读不到，但设备通电、网线插着、看起来一切正常。

**根因**：秤经交换机被接到了一台**测试路由器**的 LAN 侧，而该路由器 WAN 口接公司墙口；5090 与开发机都在公司网这一侧。秤带着静态 `192.168.0.80` 进入一个 LAN 网段并非 `192.168.0.0/24` 的网络，**既进不了那个网络（网关 `.1` 在那侧不存在），也不再与公司网同处一个二层域**，于是双向不可达。

**判据**：公司网段 `arp-scan` 中秤的 MAC `d4:ad:20:ef:cf:8d` 完全消失；同时公司网里新出现 `192.168.0.163`（TP-Link，OUI `f8:c9:03`，全端口关闭），即那台测试路由器的 WAN 口。

**教训**：

- 中间多一台**做 NAT 或换了网段的路由器**，就会把演示设备与服务器切成两个互不可达的世界。交换机是二层透明的，串多少台都无妨——**问题永远出在路由器,不在交换机**。
- 排查顺序应当是「先看设备 MAC 还在不在本二层网络」，而不是先怀疑服务、代码或秤本身。
- 非网管型交换机没有 IP，任何扫描都发现不了它，拓扑最终只能靠看线确认。

## 8. 临时形态：桥模式（不搭完整演示网时用）

只想让秤恢复、不预演现场拓扑时，把演示路由器切成 **AP / 桥模式**：关掉 DHCP，并**用 LAN 口连墙**（WAN 口空着）。它退化成交换机 + 无线接入点，所有设备回到公司网，秤仍是 `.80`、5090 仍是 `.50`，零改动。

优先用路由器自带的「接入点模式」而不是手动关 DHCP——前者会连带关掉 NAT 并把 WAN 口也变成 LAN 口，不会因插错口而重新踩坑。

代价：没有预演到独立演示网那一层。展会前务必按 §5 完整走一遍。

## 9. 违规即失败

- 把演示路由器 WAN 接公司墙口（LAN/WAN 同网段）。
- 为了迁就路由器默认网段而改 LAN 为 `192.168.1.0/24`——那等于触发 §4 全部改动，含手机 App 重新装机。
- 在 5090 上手改网络配置来适配临时网络。5090 的静态地址是本方案的锚点，要改先改本文档。
