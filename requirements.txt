# Web 服务框架
fastapi
uvicorn
python-multipart

# 官方 Gradio UI（挂在 /gradio）；本机实测 6.1.0，app.py 内有 gradio 6 兼容 shim
gradio

# 图像/数值处理
numpy
opencv-python
Pillow

# 网格 mesh 产物导出 GLB（/panel 的 mesh 模式，由深度反投影自建三角网格）
trimesh

# 深度模型推理（需 CUDA 环境）
torch

# 注意：depth_anything_3 不在 PyPI，需从本地 DA3 源码目录引入，
# 见 README「外部依赖」一节（app.py 通过 sys.path 引用 DA3/src）。
